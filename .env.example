# MCP Gateway Demo - Environment Variables (Optional)
# Copy this file to .env to customize values
# All variables have defaults, so .env is not required

# PostgreSQL Configuration
POSTGRES_PASSWORD=postgres

# Admin Credentials
ADMIN_EMAIL=admin@demo.local
ADMIN_PASSWORD='asdQWE!@#'
ADMIN_NAME=MCP Demo Administrator

# JWT Configuration (change in production!)
JWT_SECRET_KEY=demo-jwt-secret-change-in-production

# Logging
LOG_LEVEL=INFO

# Application Domain (for OAuth callbacks)
APP_DOMAIN=http://localhost:4444

# OpenWeatherMap API Key (optional - without it, weather server returns mock data)
# Get a free API key at: https://openweathermap.org/api
OPENWEATHERMAP_API_KEY=

# Shipment Data API (optional - for /shipment route)
# The token-refresh sidecar uses these to auto-fetch and refresh bearer tokens.
SHIPMENT_TOKEN_URL=
SHIPMENT_CLIENT_ID=
SHIPMENT_CLIENT_SECRET=
SHIPMENT_SCOPE=

# M2M Inspector (client_credentials auth for /inspector-m2m route)
# These match the client registered in the local IDP (Config.cs).
M2M_CLIENT_ID=m2m-inspector
M2M_CLIENT_SECRET=m2m-inspector-secret
